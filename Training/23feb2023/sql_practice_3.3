create database sql_practice_3_3;
-- Case 1
-- Consider a table called "books" with the following columns: 
-- "id", "title", "author", "publication_year". Write a SQL query 
-- to retrieve the title and author of the oldest book in the 
-- table.

CREATE TABLE sql_practice_3_3.books (
    id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(150),
    autor VARCHAR(255),
    publication_year INT
);

SELECT 
    title, author
FROM
    sql_practice_3_3.books
GROUP BY id
HAVING publication_year = MIN(publication_year);

-- Case 2
-- Create a table called "employees" with the following columns: 
-- "id" (integer, primary key), "name" (text), "age" (integer), and 
-- "salary" (integer).


create table sql_practice_3_3 . employees (
id int not null  primary key ,
emp_name varchar(255),
age int,
salary int
);
INSERT INTO sql_practice_3_3. employees (id, emp_name, age, salary)
VALUES
    (1, 'John', 35, 60000),
    (2, 'Mary', 27, 50000),
    (3, 'Peter', 42, 75000),
    (4, 'Olivia', 29, 55000),
    (5, 'Michael', 38, 80000);
	
   --  --01 --
  --   Write a SQL query to select all employees from the 
-- "employees" table.

   select * from employees;
  --  02
   --  2. Write a SQL query to select the name and salary of all 
-- employees with a salary greater than 60000.
SELECT 
    emp_name, salary
FROM
    sql_practice_3_3.employees

 WHERE salary > 6000;
--  3. Write a SQL query to update Peter's age to 43.
update sql_practice_3_3. employees
set emp_name = 'peter'
where age = 43;

-- 4. Write a SQL query to delete the employee with the id of 4.
delete from sql_practice_3_3 .employees
where id = 4;
-- 5. Write a SQL query to calculate the average salary of all 
-- employees.
select  avg(salary)as averagesalary from sql_practice_3_3 . employees ;
-- Write a SQL query to select the name and age of the oldest 
-- employee.
select emp_name,age from sql_practice_3_3 . employees
WHERE age = (SELECT MAX(age) FROM sql_practice_3_3.employees);

-- 7. Write a SQL query to select the name and age of the 
-- youngest employee.
select emp_name,age from sql_practice_3_3 . employees
WHERE age = (SELECT MIN(age) FROM sql_practice_3_3.employees);
-- 8. Write a SQL query to select the name of the employee with 
-- the highest salary.
select emp_name,salary from sql_practice_3_3 . employees
WHERE salary = (SELECT MAX(salary) FROM sql_practice_3_3.employees);
